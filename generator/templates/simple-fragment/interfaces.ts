export interface __FragmentName__Controller {
    /* State */
    example: string;
    /* Events */
    onButtonPressed: () => void;
}

export interface __FragmentName__FragmentProps {
    useController?: () => __FragmentName__Controller;
}
